<div class="row">
  <div class="col-md-4">
    <h2>Cron Jobs</h2>
  </div>
  <div class="col-md-8" style="text-align: right">
    <a href="<%= cron_job_path(:new)%>" class="btn btn-danger">Post a new cron job</a>
  </div>
</div>
<ul class="list-unstyled" style="">
  <%= for cron_job <- @cron_jobs do %>
  <li class="job-list">
    <div class="row">
      <div class="col-md-9">
        <a href="/cronjob/<%= cron_job.id %>">
          <span class="title"><%= cron_job.name %></span>
          <span class="label label-success"><%= cron_job.cron_schedule %></span>
          <span class="label label-default"><%= cron_job.command %></span>
        </a>
      </div>
      <div class="col-md-3 buttons" style="text-align: right">
        <a href="/cronjob/<%= cron_job.id %>/edit" class="btn btn-warning btn-sm">
          Edit</a> &nbsp;&nbsp;
          <a href="/cronjob/<%= cron_job.id %>?action=delete" class="btn btn-default btn-sm">
            Delete</a>
<!--          <input type="hidden" name="_method" value="DELETE">
          <button type="submit" class="btn btn-default btn-sm">Delete</button>
        -->
      </div>
    </div>
  </li>
  <% end %>
</ul>